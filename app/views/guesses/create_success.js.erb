document.querySelectorAll('[data-letter="<%= params[:guess].upcase %>"]').forEach((element) => {
  element.setHTML('<%= params[:guess].upcase %>');
});

document.querySelector('#guessed_box').setHTML(
  "<%= session[:guesses].join(', ') %>"
);